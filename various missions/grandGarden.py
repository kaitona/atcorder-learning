import sys
import math

def solve(count, flowers):
    wateringCount = 0
    flowers = [0] + flowers + [0]
    
        
    

def flowersNeedHeight(flowers):
    total = 0
    for flower in flowers:
        total += flower
    return total


data = sys.stdin.readlines()
flowerCount = int(data[0].replace(" ", ""))
flowers = list(map(int, data[1].split(" ")))

solve(flowerCount, flowers)